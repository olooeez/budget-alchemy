import { makeStyles } from "@material-ui/core"

export default makeStyles(() => ({
  radioGroup: {
    dispaly: 'flex',
    justifyContent: 'center',
    marginBottom: '-10px',
  },
  button: {
    marginTop: '20px',
  }
}))
